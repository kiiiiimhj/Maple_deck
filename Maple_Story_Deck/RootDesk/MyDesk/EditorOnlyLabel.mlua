@Component
script EditorOnlyLabel extends Component

	-- 하이어라키/씬뷰에서 육안으로 위치를 확인하기 위한 개발용 마커(예: 존 경계 번호표) — 정적 편집
	-- 화면에서는 그대로 보이고, 실제 플레이(Maker Play/배포)가 시작되면 OnBeginPlay에서 즉시 숨긴다.
	method void OnBeginPlay()
		self.Entity.Enable = false
	end

end
